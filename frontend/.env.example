# Copie para .env (ou configure direto no painel do Netlify, em
# Site settings > Environment variables) e ajuste com a URL real do backend.
#
# Em desenvolvimento, deixe em branco ou não crie este arquivo: o cliente
# usa o fallback '/api' com o proxy do Vite.

VITE_API_URL=https://seuusuario.pythonanywhere.com/api
